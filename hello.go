package main
import (
	"fmt"
	"github.com/user/stringutil"
)//lets import that library
func main() {
	fmt.Println("hello, world\n");
	fmt.Println(stringutil.Reverse("!oG, olleH"))//we are using the package we made to reverse both those strings and print them again
}
